using UnityEngine;
using System.Collections;

public class trowRope : MonoBehaviour {

	public Rigidbody ropeEnd;

	// Use this for initialization
	void Start () {
		//ropeEnd.AddForce (new Vector3(-1,1,0)*450);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
